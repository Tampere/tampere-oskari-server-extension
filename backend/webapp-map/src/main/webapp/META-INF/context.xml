<?xml version="1.0" encoding="UTF-8"?>
<!-- Context path is inferred from the path of this.file-->
<Context reloadable="false">

    <!-- Configure additional resources location if needed -->
    <!--Resources className="org.apache.catalina.webresources.StandardRoot">
        <PreResources className="org.apache.catalina.webresources.DirResourceSet"
                      base="${catalina.base}/resources/"
                      internalPath="/"
                      webAppMount="/WEB-INF/classes" />
    </Resources>
-->
    <!-- Disable session persistence across restarts -->
    <!--Manager pathname="" /-->
    <Valve className="org.apache.catalina.valves.RemoteIpValve"
           trustedProxies="193\.111\.93\.\d{1,3}|10\.\d{1,3}\.\d{1,3}\.\d{1,3}"
    />
    <Valve className="org.apache.catalina.valves.AccessLogValve" directory="logs"
           prefix="localhost_access_log" suffix=".txt" requestAttributesEnabled="true"
           pattern="%a %l %u %t &quot;%r&quot; %s %B %D &quot;%{Referer}i&quot; &quot;%{User-Agent}i&quot;"/>
</Context>